# Tasks: Shopping List Photo OCR

**Input**: Design documents from `/specs/002-shopping-list-ocr/`

**Prerequisites**: plan.md, spec.md, research.md (R1–R12), data-model.md, contracts/ (ocr, google-vision, image, shopping-list, discord, config), quickstart.md, `.specify/memory/constitution.md`

**Tests**: Constitution Principle I (Test-First) is non-negotiable for this project, so every module carries test tasks written FIRST (red), then implementation (green). No test contacts the real Vision API or Discord gateway; live behavior is validated by the quickstart smoke run.

**Organization**: Tasks grouped by user story so each story is an independently testable increment. Note: unlike 001, the stories here are **sequential** — US2 extends the orchestrator file US1 creates, and US3's parity suite exercises the US1+US2 behavior against the stub provider (see Dependencies).

## Format: `[ID] [P?] [Story?] Description (file path)`

- **[P]**: Can run in parallel (different files, no dependencies on incomplete tasks)
- **[Story]**: User story this task belongs to (US1, US2, US3); Setup/Foundational/Polish tasks have none
- Paths assume the single-package repo at root (matches plan.md Project Structure, extending the 001 layout)
- Spec identifiers (FR-XXX/SC-XXX/R-n) appear in these task descriptions for traceability only — per AGENTS.md and the Constitution workflow, implemented code comments and test names MUST describe the behavior directly and MUST NOT carry these identifiers

---

## Phase 1: Setup (Shared Infrastructure)

**Purpose**: New pinned dependency, env documentation, and the machine-enforced import boundary. No business logic.

- [ ] T001 Add `@google-cloud/vision@6.1.0` to `package.json` dependencies as an exact pin (`npm install --save-exact @google-cloud/vision@6.1.0`) — no `^`/`~` per AGENTS.md; all existing 001 pins (`discord.js@14.27.0`, `pino@10.3.1`, `zod@4.4.3`, dev deps) stay untouched; verify the install succeeds on Node 24 (the package's `engines.node >= 22` covers it, research R1)
- [ ] T002 [P] Extend `.env.example` with the OCR block per contracts/config.md: `OCR_PROVIDER=` (optional, default `none`; set to `gcp-vision` to enable recognition), `GCP_SA_KEY_PATH=` (required iff `OCR_PROVIDER=gcp-vision`; path to the service-account JSON key — the path may be logged, the key contents never), `# OCR_LANGUAGE_HINTS=` (optional, comma-separated loose BCP-47 tags; omit = provider auto-detect), `# OCR_CHANNEL_ALLOWLIST=` (optional, comma-separated 17–20-digit snowflakes; omit = every visible channel is processed), each with a one-line comment mirroring the contract's defaults
- [ ] T003 [P] Extend `biome.json` import boundaries for the new vendor library (research R12): add `"@google-cloud/vision": "Only src/google-vision may import @google-cloud/vision."` to the base `linter.rules.style.noRestrictedImports.options.paths` AND to the `paths` of every existing directory override (`src/discord/**`, `src/config/**`, `src/logger/**`, `src/health/**`, `src/lifecycle/**`, `src/echo/**`, `src/shared/**` — Biome overrides replace the base rule config, so omitting the entry there would open a hole); add ONE new override for `src/google-vision/**` forbidding `discord.js`, `pino`, and `zod` but NOT `@google-cloud/vision` (mirroring the `src/discord` pattern); the new pure modules (`src/ocr/`, `src/image/`, `src/shopping-list/`) need no override — the base rule already forbids all four libraries there. Validate with a negative check: temporarily add `import '@google-cloud/vision';` to a file under `src/shopping-list/`, confirm `npm run lint` fails, then remove it

---

## Phase 2: Foundational (Blocking Prerequisites)

**Purpose**: Deferred-work tracking, config extensions, the provider-agnostic OCR contract, canonical user-facing messages, and shared test helpers. Every user story depends on these.

**⚠️ CRITICAL**: No user story work can begin until this phase is complete.

- [ ] T004 Create the six deferred-work GitHub issues (delivery gate for FR-023/SC-009; quickstart §4 fixes this as the FIRST Phase 2 task — tracked up front, not at the end): (1) `!help` command (v1 only mentions it in the usage hint); (2) context-aware / AI-powered guidance replacing the hardcoded hint — noting that when implemented, the AI call MUST carry a system prompt that prevents prompt-injection attacks; (3) duplicate-submission de-duplication; (4) direct-message handling (requires additional gateway permissions); (5) image-format conversion (notably iPhone HEIC → JPEG; v1 forbids preprocessing); (6) operator observability/monitoring (e.g. Grafana) including an unsupported-image-format counter metric. Each issue: title, rationale, and a reference to feature `002-shopping-list-ocr`
- [ ] T005 [P] Extend `Config` in `src/shared/types.ts` with the four OCR fields per contracts/config.md: `ocrProvider: 'gcp-vision' | 'none'`; `gcpSaKeyPath: string | null`; `ocrLanguageHints: readonly string[]`; `ocrChannelAllowlist: readonly string[] | null` (null = process every channel the bot can see). Existing 001 fields unchanged
- [ ] T006 [P] Write failing config unit tests FIRST (extend `tests/unit/config.spec.ts`) per contracts/config.md Test obligations: env with only `DISCORD_TOKEN` → `ocrProvider 'none'`, `gcpSaKeyPath null`, `ocrLanguageHints []`, `ocrChannelAllowlist null`; `OCR_PROVIDER=gcp-vision` without `GCP_SA_KEY_PATH` → `ConfigError` naming `GCP_SA_KEY_PATH`, reason `missing`; with the key path set → loads; `OCR_PROVIDER=bogus` → `malformed` naming `OCR_PROVIDER`; `OCR_LANGUAGE_HINTS=en,de,zh-Hans,en-t-i0-handwrit` → parsed verbatim (the handwriting form MUST pass); an entry with spaces or digits-first → `malformed`; absent → `[]`; `OCR_CHANNEL_ALLOWLIST` with two 18-digit ids → parsed array; `abc` or `123` → `malformed`; explicit empty string → `malformed` (an empty allowlist would silently disable OCR everywhere); absent → `null`; provider `none` + key path present → loads and stores the path (operator pre-staging). Run and confirm RED (new fields missing), and confirm the full 001 config suite still passes unchanged
- [ ] T007 Implement the config extension in `src/config/schema.ts` + `src/config/load-config.ts` to make T006 green (depends on T005, T006): validation rules exactly as enumerated in T006 — hints entries match `/^[A-Za-z]{2,3}(-[A-Za-z0-9]{1,8})*$/`, allowlist entries match `/^\d{17,20}$/`; the new fields validate AFTER all 001 fields, in the documented table order (`OCR_PROVIDER`, `GCP_SA_KEY_PATH`, `OCR_LANGUAGE_HINTS`, `OCR_CHANNEL_ALLOWLIST`), with the cross-field `GCP_SA_KEY_PATH` rule evaluated at its position so the first-error contract stays deterministic; config remains pure I/O-free (key-file readability is NOT checked here — the provider checks it at construction); returned `Config` stays frozen; a 001-era env file still loads, yielding `ocrProvider: 'none'`
- [ ] T008 [P] Create `src/ocr/types.ts` per contracts/ocr.md public surface (zero vendor imports, nothing outside `src/shared/types`): `RecognizedLine { text: string; confidence: number /* [0,1] */; boundingBox: { x; y; width; height } /* px */ }`; `Recognition { text: string /* whole-page, provider's own break semantics */; lines: RecognizedLine[] /* ordered */ }` with the fidelity invariant `lines.map(l => l.text).join('\n') === text` except one trailing empty line from a terminal break; `OcrImageFormat = 'jpeg' | 'png' | 'webp'`; `UnavailableCause = 'unreachable' | 'unauthorized' | 'quota-exhausted' | 'provider-error' | 'deadline-exceeded' | 'disabled'`; `OcrProviderResult = { status: 'ok'; recognition } | { status: 'undecodable-image' } | { status: 'unavailable'; cause }`; `OcrProvider` interface with `readonly id: string` and `recognize(req: { image: { bytes: Uint8Array; format: OcrImageFormat }; languageHints: readonly string[]; timeoutMs: number }): Promise<OcrProviderResult>`
- [ ] T009 [P] Write failing `ocr` contract tests FIRST in `tests/contract/ocr.spec.ts` per contracts/ocr.md Test obligations (depends on T008 for types): fidelity-invariant property check over a table of crafted `Recognition` fixtures asserting `lines.map(l => l.text).join('\n') === text` (this guards the fixtures the google-vision mapper tests rely on); `createDisabledProvider()` — `id === 'disabled'`, every call resolves `{ status: 'unavailable', cause: 'disabled' }`, and performs NO network access (run against a sabotaged global `fetch` that throws if called). Confirm RED (module missing)
- [ ] T010 Implement `src/ocr/disabled-provider.ts` to make T009 green (depends on T008, T009): `createDisabledProvider(): OcrProvider` returning the always-unavailable provider — this is how a disabled configuration still answers every image submission with the generic message instead of silence (FR-025)
- [ ] T011 [P] Create the shared test helpers in `tests/helpers/` (depends on T008): `stub-ocr-provider.ts` — a stub implementing `OcrProvider` with per-call scripted results (`ok` with lines/text honoring the fidelity invariant, `undecodable-image`, and every `UnavailableCause`), recording call order and arguments (bytes/format/languageHints/timeoutMs) for sequencing and budget assertions; `scripted-fetch.ts` — canned `Response`-like objects for the image module's fetch seam (configurable status, headers with `Content-Length` present/absent, chunked bodies via `ReadableStream`), recording requested urls and whether the body reader was cancelled
- [ ] T012 [P] Write failing messages unit tests FIRST in `tests/unit/messages.spec.ts`: byte-equality assertions for the five canonical `LIST_MESSAGES` strings — `serviceUnavailable: 'Service is not available, please try again later or contact the admin.'`, `noReadableText: "I couldn't read any text — can you try a different photo?"`, `unsupportedFormat: 'Sorry, that file format is not supported — please send a JPEG, PNG, or WEBP photo.'`, `imageTooLarge: 'That image is too large — please send a photo under 7 MB.'`, `busy: 'I am still working on your previous list — please wait for it to finish before sending another.'` — and for `usageHintMessage(commandPrefix)` returning exactly `` `Send a photo of your shopping list and I will reply with the recognized text. Type ${commandPrefix}help for information on how to use the bot.` ``. Confirm RED
- [ ] T013 Implement `src/shopping-list/messages.ts` to make T012 green (depends on T012): the frozen `LIST_MESSAGES` constants and `usageHintMessage(commandPrefix: string): string` exactly per contracts/shopping-list.md (CANONICAL strings — tests assert byte-equality; all are far under 2000 chars and never split)

**Checkpoint**: Foundation ready — OCR env config loads, the provider contract + disabled provider + test stub exist, and the canonical user-facing strings are pinned.

---

## Phase 3: User Story 1 - Send a Photo, Get the List Text Back (Priority: P1) 🎯 MVP

**Goal**: A user posts a handwritten-list photo in a processed channel; the bot downloads it, runs Google Cloud Vision document text detection, and replies in the same channel with the recognized text — byte-faithful, in the provider's line order, joined by newlines — handling multi-image messages in attachment order, replying with the usage hint to image-less messages, a distinct message for no-text images, splitting >2000-char replies at line boundaries with `…` continuation markers, and neutralizing mentions at the transport without altering text bytes.

**Independent Test**: Drive a hand-built `Events.MessageCreate` (with attachment) through the real adapter + real handler against the stubbed Discord client (`tests/helpers/stubbed-client.ts`), a scripted fetch, and the stub provider returning known page text; assert posted reply bytes equal the stub's text (multi-image: joined in attachment order), chunks ≤ 2000 with markers, `allowedMentions { parse: [], users: [], roles: [] }` on every send, usage hint for a text-only message, and correlation-id-bound lifecycle logs with no text/bytes (`tests/integration/list-ocr.spec.ts`).

### Tests for User Story 1 (write FIRST, confirm RED)

- [ ] T014 [P] [US1] Unit tests for line reconstruction in `tests/unit/to-recognition.spec.ts` per contracts/google-vision.md Test obligations (crafted annotation fixtures, no network/client): multi-paragraph input with `LINE_BREAK` and `EOL_SURE_SPACE` → ordered lines where `lines.join('\n')` equals the fixture's `text` byte-for-byte; `SPACE`/`SURE_SPACE` mid-line → single space, no line split; `HYPHEN`/`UNKNOWN`/absent break → empty string (no synthesized characters); `isPrefix: true` break emitted BEFORE the symbol; terminal break → exactly one trailing empty line dropped; word confidences → char-weighted line confidence (hand-computed expectation; a line with no confidence-bearing words reports `0`); word `boundingBox.vertices` → axis-aligned union envelope `{x, y, width, height}`; empty/missing annotation → `ok`-shaped empty recognition
- [ ] T015 [P] [US1] Unit tests for the gRPC error taxonomy in `tests/unit/map-google-error.spec.ts` — one case per mapping row (research R6): `code 3` + message containing `Bad image data` → `undecodable-image`; `code 3` WITHOUT that signature → `unavailable`/`provider-error` (conservative); `code 4` → `unavailable`/`deadline-exceeded`; `code 7` → `unavailable`/`unauthorized`; `code 8` → `unavailable`/`quota-exhausted`; `code 14` → `unavailable`/`unreachable`; `code 16` → `unavailable`/`unauthorized`; any other code → `unavailable`/`provider-error`; a non-Error throw → `unavailable`/`provider-error`; the result carries the numeric code and `reason` (when present) for log context, never secrets. Confirm RED
- [ ] T016 [P] [US1] Contract tests for the provider in `tests/contract/google-vision.spec.ts` per contracts/google-vision.md, using a stubbed `ImageAnnotatorClient` (constructor seam): request carries feature `DOCUMENT_TEXT_DETECTION` with inline `image.content` bytes pass-through; `languageHints` forwarded as `imageContext.languageHints` when non-empty, omitted when empty; gax call options carry `{ timeout: req.timeoutMs, retry: null }` (the client default 600 s total timeout + retries MUST be disabled, research R7); in-band `responses[0].error` with `code 3`/`Bad image data` → `undecodable-image`, NOT empty text; `responses[0].fullTextAnnotation` → `toRecognition`; missing/empty annotation → `ok` with empty recognition; a thrown `GoogleError` → the `mapGoogleError` taxonomy (never leaks across the module boundary); construction with a nonexistent `keyFile` throws AT CONSTRUCTION with a message naming `GCP_SA_KEY_PATH` and never containing key contents; NO test contacts the real Vision API. Confirm RED
- [ ] T017 [P] [US1] Unit tests for magic-byte sniffing in `tests/unit/sniff-format.spec.ts` per contracts/image.md (research R9): minimal valid JPEG (`FF D8 FF`), PNG (`89 50 4E 47 0D 0A 1A 0A`), WEBP (`RIFF` at offset 0 AND `WEBP` at offset 8) → correct format; GIF, BMP, TIFF (both endians), HEIC `ftyp` box, random bytes, fewer than 12 bytes → `null`. Confirm RED
- [ ] T018 [P] [US1] Contract tests for image download in `tests/contract/image.spec.ts` per contracts/image.md, using the scripted fetch seam (`tests/helpers/scripted-fetch.ts`, T011): `reportedSize > MAX_IMAGE_BYTES` (`7 * 1024 * 1024`) → `too-large` and fetch NEVER called; contentType `image/gif` / `application/pdf` / `video/mp4` → `unsupported-format`, fetch never called; contentType `null` → proceeds to download; fetch rejects or status 403/404/500 → `unretrievable`; `Content-Length` over cap → `too-large` with the body reader cancelled; chunked stream (no `Content-Length`) exceeding the cap mid-body → `too-large` with the reader cancelled (accumulation bounded — assert NOT the whole body buffered); PNG magic bytes with contentType claiming `image/webp` → `ok` with format `png` (bytes beat the hint); JPEG body under cap → `ok` with `sizeBytes` equal to the actual length. Confirm RED
- [ ] T019 [P] [US1] Unit tests for reply splitting in `tests/unit/split-reply.spec.ts` per contracts/discord.md §3 (research R10): text ≤ limit (default 2000) → single chunk byte-identical; text with line boundaries → every chunk ≤ limit, no line split across chunks, concatenation of chunks equals the original (line-boundary splits add nothing); one 5000-char single line → chunks ≤ limit, every cut chunk except the last ends with `…`, every continuation chunk except the first starts with `…`, and stripping the markers reconstructs the original byte-for-byte; a window whose only newline is at position 0 → treated as mid-line (marker path); a custom `limit` argument is honored. Confirm RED
- [ ] T020 [P] [US1] Contract tests for the orchestrator happy path in `tests/contract/shopping-list.spec.ts` per contracts/shopping-list.md, using the stub provider + a scripted `fetchImage` (zero network, zero vendor SDKs): single image → `recognize` called once with the fetched bytes/format, the configured language hints, and a positive `timeoutMs`; reply text equals the stub's page text byte-for-byte; three images → provider called sequentially in attachment order (assert call order) and reply = the three page texts joined by a single `'\n'` in order; a failure on image 2 aborts the submission — image 3 is never downloaded nor recognized and exactly one failure message is produced, no partial text (all-or-nothing); `ok` with combined text empty or whitespace-only → `noReadableText`; `fetchImage`/provider throwing → caught and answered with `serviceUnavailable` (unstructured outcomes never leak); a spy logger receives `received`/`submitted`/`succeeded` transitions bound to the correlation id and NO recognized-text or byte fields. Confirm RED
- [ ] T021 [P] [US1] Contract tests for adapter routing in `tests/contract/discord-routing.spec.ts` per contracts/discord.md §1–§2 (stubbed `Client` via the existing `clientFactory` seam, scripted `listSubmission`): echo command in a non-allowlisted channel → echo reply, `listSubmission` never called, no usage hint (commands are NOT subject to the allowlist and never trigger the hint, even with attachments); image message in a non-allowlisted channel → nothing sent, `listSubmission` never called; allowlist `null` → every channel processed; message with a PDF attachment → `listSubmission` IS called with the attachment descriptor (filtering is not the adapter's job); descriptors preserve message order and map `url` (never `proxyURL`) / `size → reportedSize` / `contentType → reportedContentType`; text-only message in a processed channel → usage-hint reply, `listSubmission` never called; handler result > 2000 chars → multiple sends in order, each ≤ 2000, each with empty `allowedMentions`; handler throws → the 001 canonical internal-error reply; EVERY send in EVERY scenario carries `allowedMentions { parse: [], users: [], roles: [] }`. Confirm RED
- [ ] T022 [P] [US1] Contract tests for lifecycle wiring in `tests/contract/lifecycle-ocr-wiring.spec.ts` (stub the `google-vision` and `ocr` modules): `OCR_PROVIDER=gcp-vision` → provider built via `createGoogleVisionProvider` with the configured key path; `OCR_PROVIDER=none` or absent → `createDisabledProvider`; the handler is constructed with the configured `ocrLanguageHints` and the real `fetchAndValidateImage`; the adapter receives `usageHint` pre-interpolated from `config.commandPrefix` and the constructed `listSubmission` handler; provider construction failure (bad key path) → the 001 post-config-throw fatal pattern (one `fatal` naming the subsystem, exit 1). Confirm RED
- [ ] T023 [P] [US1] Integration tests for the full flow in `tests/integration/list-ocr.spec.ts`: a hand-built `Events.MessageCreate` with one image attachment driven through the REAL adapter + REAL handler with the stubbed Discord client, scripted fetch, and stub provider → reply posted to the originating channel byte-identical to the stub's text (never a DM, never cross-posted); recognized text containing `@everyone` / `<@id>` mention tokens → posted byte-for-byte with empty `allowedMentions` (neutralized at transport, text unaltered); a two-image message → combined text in attachment order; a text-only message in a processed channel → the usage hint naming `!help` (with prefix `!`); a > 2000-char recognition → ordered chunks each ≤ 2000 with `…` markers at mid-line cuts; two concurrent submissions from different users in different channels → each receives its own text in its own channel (no cross-talk). Confirm RED

### Implementation for User Story 1

- [ ] T024 [P] [US1] Implement `src/google-vision/to-recognition.ts` (pure; depends on T014, green it): export `toRecognition(annotation: FullTextAnnotationLike): Recognition` walking `pages → blocks → paragraphs → words → symbols`, appending each symbol's text then the translation of its trailing `property.detectedBreak.type` — `SPACE`/`SURE_SPACE` → `' '`, `EOL_SURE_SPACE`/`LINE_BREAK` → `'\n'`, `HYPHEN`/`UNKNOWN`/absent → `''` — honoring `isPrefix: true` (emitted before the symbol); lines = the reconstructed string split on `'\n'`, dropping exactly one trailing empty line if present; per-line `confidence` = char-weighted mean of constituent word confidences (research R4); per-line `boundingBox` = axis-aligned union of word `boundingBox.vertices` in page pixel space (research R5); the fidelity invariant `lines.join('\n') === text` MUST hold
- [ ] T025 [P] [US1] Implement `src/google-vision/map-google-error.ts` (pure; depends on T015, green it): export `mapGoogleError(err: unknown): OcrProviderResult` with the exact mapping table from T015 (research R6); applicable to both a thrown `GoogleError` and an in-band `responses[0].error` (the `google.rpc.Status` carries the same numeric `code`/`message` shape); the numeric code and `reason` (when present) are carried on the result for log context; secrets never are
- [ ] T026 [P] [US1] Implement `src/discord/split-reply.ts` (pure; depends on T019, green it): export `splitReply(text: string, limit = 2000): string[]` — `text.length ≤ limit` → `[text]`; otherwise fill each chunk up to `limit` preferring the LAST `'\n'` inside the window (split after it; a newline at window position 0 does not count as a line boundary); no usable boundary → split mid-line at `limit - 1`, end the chunk with `…` and start the next with `…` (research R10)
- [ ] T027 [P] [US1] Implement `src/image/sniff-format.ts` (pure; depends on T017, green it): export `sniffFormat(bytes: Uint8Array): ImageFormat | null` checking the first 12 bytes — JPEG `FF D8 FF`; PNG `89 50 4E 47 0D 0A 1A 0A`; WEBP `RIFF` at offset 0 AND `WEBP` at offset 8; fewer than 12 bytes or anything else (GIF/BMP/TIFF/HEIC `ftyp`/unknown) → `null`; no sniffing library (three 12-byte checks, research R9)
- [ ] T028 [US1] Implement `src/image/fetch-image.ts` (depends on T018, T027): export `MAX_IMAGE_BYTES = 7 * 1024 * 1024` (hardcoded, NOT config) and `fetchAndValidateImage(input: ImageFetchInput, fetchImpl = global fetch): Promise<ImageFetchResult>` with the fixed evaluation order from contracts/image.md §1 — (1) `reportedSize > MAX_IMAGE_BYTES` → `too-large`, no download; (2) `reportedContentType` present and not `image/jpeg | image/png | image/webp` → `unsupported-format`, no download (a MISSING content type proceeds); (3) fetch `url` (never `proxyURL`, research R8); network error or non-OK status → `unretrievable`; (4) `Content-Length` over cap → abort, `too-large`; (5) stream via `res.body.getReader()` accumulating chunks, cancel the reader the moment the running total exceeds the cap → `too-large` (never buffer oversize content); (6) `sniffFormat` on the bytes: `null` → `unsupported-format`; (7) else `ok { bytes, format, sizeBytes }`. Bytes are returned to the caller and never copied, stored, or logged (log url host + reported size at most)
- [ ] T029 [US1] Implement `src/google-vision/provider.ts` (depends on T016, T024, T025): export `createGoogleVisionProvider(deps: { keyFile: string }): OcrProvider` — validates the key file's existence/readability AT CONSTRUCTION (startup) and throws naming `GCP_SA_KEY_PATH` (never at first photo, research R2; message never contains key contents); constructs `new ImageAnnotatorClient({ keyFilename: keyFile })` internally behind a constructor seam for tests; `recognize()` issues ONE `documentTextDetection` request with inline `image.content` bytes, `imageContext.languageHints` only when non-empty, and gax call options `{ timeout: req.timeoutMs, retry: null }`; reads `responses[0]`: in-band `.error` → `mapGoogleError`; `.fullTextAnnotation` → `toRecognition`; missing/empty annotation → `ok` with empty recognition (the no-readable-text decision belongs to the orchestrator); thrown errors → `mapGoogleError`; never leaks `GoogleError` across the module boundary; no retention, no content logging. This is the ONLY file importing `@google-cloud/vision` (T003 enforces)
- [ ] T030 [US1] Implement the first iteration of `createListSubmissionHandler` in `src/shopping-list/handle-list-submission.ts` (depends on T020, T028, plus T008/T013 from Foundational): export `SUBMISSION_BUDGET_MS = 25_000` (hardcoded, NOT config) and the types `SubmissionAttachment { url; reportedSize; reportedContentType }`, `ListSubmissionInput { correlationId; userId; channelId; attachments /* message order = processing order */ }`, `ListReply { text }`, `ListSubmissionHandler`; `createListSubmissionHandler(deps: { provider; fetchImage; languageHints; logger; now = Date.now })` — the logger is injected (type from `src/logger`; the module NEVER imports pino). Behavior this iteration: `deadline = now() + SUBMISSION_BUDGET_MS` at start; per attachment in order: `fetchImage` → `provider.recognize({ image: { bytes, format }, languageHints, timeoutMs: deadline - now() })`; ANY non-ok fetch result, non-ok provider result, or thrown exception aborts the whole submission with `serviceUnavailable` (the refined per-outcome taxonomy, local metadata pre-checks, and busy guard come in US2 — structure the code so they slot in); success assembly: reply text = the per-image `recognition.text` values in attachment order joined with a single `'\n'` — no trimming, reordering, merging, or normalization anywhere; the assembly code carries the comment that when AI interpretation is added, the AI call MUST include a system prompt that prevents prompt-injection attacks (FR-012 hook); combined text empty or whitespace-only → `noReadableText` (a blank page among recognized pages never triggers it); log `received` (userId, channelId, attachmentCount, reportedSizes), `submitted` (per image: position, sizeBytes, format), `succeeded` (imageCount, elapsedMs), `failed` — every line bound to the correlation id, NEVER containing image bytes, recognized text, attachment urls beyond host, or credential material; after the reply is computed, drop every reference to bytes and recognition output
- [ ] T031 [US1] Extend `src/discord/adapter.ts` per contracts/discord.md (depends on T021, T023, T026, T030): `createDiscordAdapter` deps gain `listSubmission: ListSubmissionHandler` and `usageHint: string` (pre-interpolated by lifecycle from `config.commandPrefix`); routing order inside `onMessageCreate` after the existing bot-author and `stopping` guards — (1) recognized command (`content` starts with `commandPrefix` + `echoCommandName`) → the existing echo flow UNCHANGED; commands are not subject to the channel allowlist and never trigger the usage hint, even when the message also carries attachments; (2) channel allowlist: `config.ocrChannelAllowlist` non-null and `message.channelId` not in it → ignore the message entirely (no hint, no OCR); null → every visible channel is processed; (3) message with ≥ 1 attachment of ANY kind → mint a correlation id via `newCorrelationId()`, call `listSubmission({ correlationId, userId, channelId, attachments })` mapped from the message's attachments in message order (`url` — never `proxyURL`, `size → reportedSize`, `contentType → reportedContentType`), then send the resulting reply through `splitReply` chunks sequentially with `await`, in order; non-image attachments are NOT filtered here (the image/format gates reject them); (4) any other message → the usage-hint reply. Every reply goes through the 001 `sendWithRetry` with `allowedMentions { parse: [], users: [], roles: [] }` — mention neutralization at the transport, text bytes never altered; replies only to the originating channel (never DM, never cross-post); handler throws → the 001 canonical internal-error reply; NOTHING else changes (connection lifecycle, reconnect correlation logging, bounded retry, shutdown — the 001 suites stay green; the only new discord.js surface is reading `message.attachments`)
- [ ] T032 [US1] Extend `src/lifecycle/run-app.ts` wiring (depends on T022, T029, T030, T031, and Foundational T007/T010): after `loadConfig`, build the provider per config — `ocrProvider === 'gcp-vision'` → `createGoogleVisionProvider({ keyFile: config.gcpSaKeyPath })` (construction validates the key file; a throw follows the existing post-config fatal pattern, failing at startup naming `GCP_SA_KEY_PATH`), `'none'` → `createDisabledProvider()`; construct `listSubmission = createListSubmissionHandler({ provider, fetchImage: fetchAndValidateImage, languageHints: config.ocrLanguageHints, logger: <validated-logger child>, now: Date.now })`; compute `usageHint = usageHintMessage(config.commandPrefix)`; pass `listSubmission` + `usageHint` into `createDiscordAdapter` alongside the existing 001 deps. The swap surface stays exactly: new provider module + config enum value + this one wiring branch

**Checkpoint**: User Story 1 fully functional — photo in → byte-faithful recognized text out (single and multi-image), usage hint, no-text message, long-reply splitting, mention neutralization, allowlist, all wired end to end; testable against the stub provider with zero external calls.

---

## Phase 4: User Story 2 - The Bot Tells Me Clearly When It Cannot Read My List (Priority: P2)

**Goal**: Every non-happy outcome maps to exactly one of the three message classes: ONE generic message for all service-side failures (unreachable, unauthorized, quota exhausted, provider error, disabled, budget exceeded, unretrievable) with the specific cause logged under the correlation id; DISTINCT actionable messages per input problem (no readable text, unsupported/corrupt format, image too large) rejected locally before any provider call where possible; a DISTINCT busy message when the same user submits while a previous list is in flight — checked AFTER local format/size checks. Multi-image submissions are all-or-nothing; budget overruns abandon client-side and keep the bot responsive.

**Independent Test**: Against the stub provider + scripted fetch, simulate every outcome class and observe: the identical generic message for all service-side causes (no technical detail, no crash, no stuck request), a distinct message per input problem, the busy message for a second concurrent same-user submission (while an oversized attachment from a busy user still earns the too-large message), and cancellation-with-generic-reply when the 25 s budget passes mid-submission — with content-free transition logs throughout.

### Tests for User Story 2 (write FIRST, confirm RED against the US1 handler)

- [ ] T033 [P] [US2] Contract tests for failure classes and the busy guard in `tests/contract/shopping-list-failures.spec.ts` per contracts/shopping-list.md Test obligations (stub provider + scripted `fetchImage`): local metadata checks BEFORE processing and before guard acquisition — oversize `reportedSize` on attachment 2 of 2 → `imageTooLarge`, provider never called AND busy map untouched; bad `reportedContentType` → `unsupportedFormat`, no download; busy guard — two overlapping submissions for the same user: the first completes uninterrupted, the second gets `busy` and its provider/fetch are NEVER called; a third submission after the first finished is accepted (guard released — including when handling throws); a busy user's submission with an oversized attachment gets `imageTooLarge`, NOT `busy` (checks precede the guard); post-download `fetchImage` failures → matching replies: `too-large` → `imageTooLarge`, `unsupported-format` → `unsupportedFormat`, `unretrievable` → `serviceUnavailable`; provider arms: `undecodable-image` → `unsupportedFormat`; EACH `UnavailableCause` (`unreachable`, `unauthorized`, `quota-exhausted`, `provider-error`, `deadline-exceeded`, `disabled`) → `serviceUnavailable`; all-or-nothing: a failure on image 2 → exactly one failure message, no partial text, image 3 untouched; budget: the `now()` seam advanced past the deadline mid-submission → the `timeoutMs <= 0` path → `serviceUnavailable`, guard released, handler resolves (no stuck promise); disabled provider: every image submission → `serviceUnavailable`, still replied (never silent); concurrent users: two users' overlapping submissions both complete with their own texts (no cross-talk, per-user stub scripting)
- [ ] T034 [P] [US2] Contract tests for transition logging in `tests/contract/shopping-list-logging.spec.ts` (spy logger): one structured line per transition — `received` (userId, channelId, attachmentCount, reportedSizes), `submitted` (per image: position, sizeBytes, format), `succeeded` (imageCount, elapsedMs), `failed` (reason class: too-large / unsupported / unretrievable / provider-cause), `cancelled` (elapsedMs), `rejected-busy` — each bound to the correlation id; scan all logged values asserting NO image bytes, NO recognized text (fixture-text scan), NO attachment urls beyond host, NO credential material (a key PATH is the most sensitive value allowed)
- [ ] T035 [P] [US2] Integration tests for failure classes in `tests/integration/list-ocr-failures.spec.ts` (message-in → reply-out via the stubbed Discord client + stub provider): every service-side cause — including disabled via the wiring with `OCR_PROVIDER` unset — produces the SAME single generic message with no technical detail and no crash/stuck request (SC-003); each input problem (no readable text, unsupported/corrupt format, oversize) produces its OWN distinct actionable message, tellable apart from the generic and busy messages (SC-008); oversize and locally-unsupported files are rejected with zero provider calls; a same-user second photo while the first is in flight → busy message while the first completes normally (SC-010); two concurrent users in different channels → each gets their own text in their own channel (SC-006); a multi-image submission whose page 2 fails → single failure message, no partial text (SC-003/FR-004)

### Implementation for User Story 2

- [ ] T036 [US2] Extend `src/shopping-list/handle-list-submission.ts` to make T033–T035 green (depends on T030, T033, T034, T035) per contracts/shopping-list.md §1 fixed evaluation order: (1) local metadata checks for ALL attachments in message order — `reportedSize` over the 7 MB ceiling (`MAX_IMAGE_BYTES` from `src/image/fetch-image.ts`) → `imageTooLarge` and stop; `reportedContentType` present and outside `image/jpeg | image/png | image/webp` → `unsupportedFormat` and stop — with NO download, NO provider call, and NO guard acquisition, so a persistently-bad sender always gets the actionable message even while busy; (2) busy guard: an in-memory `Map<userId, correlationId>` — if the user already has a submission in flight → `busy` reply and stop (never queued); otherwise record the entry; the guard is released in a `finally`, ALWAYS, including on throw, and is the ONLY shared mutable state; (3) `deadline = now() + SUBMISSION_BUDGET_MS` at guard acquisition; `timeoutMs <= 0` → treat as deadline-exceeded → `serviceUnavailable` with the `cancelled` transition logged (client-side abandon; no remote cancellation exists); (4) refined outcome mapping: fetch `too-large` → `imageTooLarge`, `unsupported-format` → `unsupportedFormat`, `unretrievable` → `serviceUnavailable`; provider `undecodable-image` → `unsupportedFormat`; `unavailable` (any cause) → `serviceUnavailable` with the cause + provider id logged; thrown exceptions caught, logged with the correlation id → `serviceUnavailable`; (5) full transition logging per T034 including `rejected-busy` and `cancelled` (elapsedMs) and `failed` reason classes; different users remain fully concurrent and independent (FR-016)

**Checkpoint**: User Stories 1 AND 2 both work — the happy path plus the complete three-class outcome taxonomy, busy guard, shared budget, and content-free observability.

---

## Phase 5: User Story 3 - Operators Can Switch OCR Providers Without Changing Behavior (Priority: P3)

**Goal**: Prove the durability requirement: the active provider is swappable by configuration + a new module honoring the `OcrProvider` contract, with ZERO edits to image-detection, ordering, or reply-posting logic, and identical user-facing behavior (SC-004).

**Independent Test**: Run the complete user-facing flow (real adapter + real handler) against the stub provider from `tests/helpers/` and confirm every behavior — trigger, reply bytes, order preservation, all message classes — is identical to the real-provider suites, with zero external calls; plus a static import scan proving the vendor SDK is confined to `src/google-vision/` and no list-handling module knows the provider exists.

### Tests & Documentation for User Story 3

- [ ] T037 [P] [US3] Contract tests for provider-agnostic swap in `tests/contract/ocr-swap.spec.ts`: the full user-facing flow (real adapter + real handler + the stub `OcrProvider`, scripted fetch seam) runs with ZERO external calls — the scripted fetch asserts no unscripted url is ever requested and the stub records every `recognize` call; behavior-parity assertions: happy-path reply bytes, each failure-class message, busy rejection, and usage hint all identical to the US1/US2 expectations; static import scan (mirroring 001's `tests/contract/no-persistence.spec.ts` pattern): `@google-cloud/vision` is imported ONLY under `src/google-vision/`, and `src/shopping-list/`, `src/image/`, `src/discord/` contain no import of `src/google-vision` — a provider swap = a new sibling module implementing `OcrProvider` + one config enum value + one lifecycle wiring branch, nothing else
- [ ] T038 [P] [US3] Extend `docs/deployment.md` with the OCR operations section per quickstart §2: Google Cloud provisioning (create project, enable billing, enable the Vision API, create a service account, download its JSON key to the VPS — e.g. `/etc/empty-cart/gcv-key.json`, mode `0600`, owned by the service user); the OCR env-var reference (`OCR_PROVIDER`, `GCP_SA_KEY_PATH`, `OCR_LANGUAGE_HINTS`, `OCR_CHANNEL_ALLOWLIST` with defaults and validation rules); `OCR_PROVIDER=none` behavior (every image submission still gets the generic message — never silence); boot-time failure behavior (a missing/unreadable key file fails startup naming `GCP_SA_KEY_PATH`, not first-photo-time); and the provider-swap procedure (new module implementing the `OcrProvider` contract + config enum value + one wiring branch in `run-app.ts` — no edits to image-detection, ordering, or reply-posting logic)

**Checkpoint**: All three user stories independently functional — swap-by-configuration proven against the stub with zero external calls.

---

## Phase 6: Polish & Cross-Cutting Concerns

**Purpose**: End-to-end validation gates that tie all stories together.

- [ ] T039 [P] Add the end-to-end log scan in `tests/integration/log-scan.spec.ts` (SC-005, mirroring 001's `tests/integration/logger-redaction.spec.ts` pattern; reuses `tests/helpers/stream-logger.ts`): run representative full lifecycles through the REAL logger to a captured sink — happy recognition, each failure class, busy rejection, disabled provider — and assert zero log lines contain recognized fixture text, image byte patterns, or key material (a key PATH may appear; contents never)
- [ ] T040 Run the full verification gate per AGENTS.md: `npm run typecheck` (src + `tsconfig.test.json`), `npm test` (unit + contract + integration — the complete 001 suites must still pass unchanged), and `npm run lint` (including the new `@google-cloud/vision` boundary from T003); fix any violations before proceeding
- [ ] T041 Walk `quickstart.md` §1 automated validation, then §3 live smoke scenarios 1–13 against real Discord + real Vision: (1) handwritten-list photo → recognized text within ~30 s, lines in order, no pings rendered (SC-001/SC-002); (2) photo containing `@everyone` + a username mention → verbatim text rendered as plain text; (3) blank/unreadable photo → the no-text message; (4) PDF/GIF → unsupported-format message, logs show NO provider call; (5) image > 7 MB → too-large message, no provider call; (6) two photos in ONE message → both texts in attachment order; (7) photo then immediate second photo → busy message, first completes; (8) second user in another channel → both isolated (SC-006); (9) plain text in a processed channel → usage hint naming `!help`; (10) `!echo hello` anywhere (incl. non-allowlisted channel) → echo works, no hint; (11) revoke service account / disable Vision API → generic message, logs carry cause + correlation id, bot stays responsive (restore credentials after); (12) long list > 2000 chars → ordered chunks with `…` mid-line marker (SC-007); (13) `OCR_PROVIDER=none` → generic message, never silence. Cross-check every submission in `journalctl -u empty-cart`: received/submitted/succeeded|failed with a correlation id and NO text/bytes (SC-005). Then confirm the §4 delivery gate: all six deferred-work GitHub issues from T004 exist (SC-009); record any deviations as follow-up tasks
- [ ] T042 VPS validation per quickstart §5 (Constitution workflow §7 — sequential manual gate after T041): deploy to the real systemd-supervised VPS per `docs/deployment.md` (provision the key file), then exercise live smoke scenarios 1–3 and 11 against the production deployment, not only locally; the feature is not "done" until these pass; record results and deviations

---

## Dependencies & Execution Order

### Phase Dependencies

- **Setup (Phase 1)**: No dependencies — start immediately (T002/T003 are file-independent of T001 and can run in parallel with it)
- **Foundational (Phase 2)**: Depends on Setup — BLOCKS all user stories (config fields, OCR contract types, disabled provider, stub helpers, and canonical messages are consumed everywhere)
- **US1 (Phase 3)**: After Foundational
- **US2 (Phase 4)**: T036 extends T030's file → US2 implementation strictly follows US1's handler; US2 TESTS (T033–T035) can be written (red) while US1 implementation is in flight, since the handler module exists from T030
- **US3 (Phase 5)**: T037's parity suite exercises US1+US2 behavior through the stub → after US2; T038 (docs) only needs the wiring shape from T032 and can proceed earlier
- **Polish (Phase 6)**: After all three stories; T041 requires T038 (operator provisioning docs) and T004 (issues); T042 last

### Task-Level Dependency Graph

```text
T001 ─┬─ T002 [P]
      └─ T003 [P]
T004 [P, independent]
T005 ─┐
T006 ─┴→ T007          T008 ─┬→ T009 → T010
                             ├→ T011 [P]
T012 → T013                  └───────────────┐
US1 tests T014–T023 [all P] ← T008/T011/T013 ┘
T014 → T024 ─┐
T015 → T025 ─┼→ T029 ──────────────┐
T017 → T027 → T018 → T028 ─┐       │
T019 → T026 ───────────────┼→ T030 → T031 → T032 (also ← T022, T029, T007, T010)
T020 ──────────────────────┘
US2: T033/T034/T035 [P, red vs T030] → T036 (← T030)
US3: T037 [P] (← T036, T031), T038 [P] (← T032)
Polish: T039 [P] (← T036) → T040 → T041 → T042
```

### Within Each User Story

- Tests written FIRST, confirmed RED before implementation (Constitution Principle I)
- Pure mappers (`to-recognition`, `map-google-error`, `split-reply`, `sniff-format`) before I/O-bearing modules (`fetch-image`, `provider`) before the orchestrator before the adapter before lifecycle wiring
- A story is "done" when its Independent Test passes against the real in-process modules, not only mocks

### Parallel Opportunities

- Phase 1: T002, T003 in parallel (and with T001 — different files)
- Phase 2: T004, T005, T006, T008, T011, T012 all touch distinct files; sequential pairs only where a test greens its implementation (T006→T007, T009→T010, T012→T013)
- Phase 3: ALL ten test tasks T014–T023 in parallel; then the four pure impls T024–T027 in parallel; then the chain T028/T029 → T030 → T031 → T032
- Phase 4: T033, T034, T035 in parallel; T036 sequential after them
- Phase 5: T037 and T038 in parallel
- Phase 6: T039 parallel with US3 tail; T040–T042 strictly sequential (manual gates)

---

## Parallel Example: User Story 1

```text
# Launch all US1 tests together (all [P], different files):
T014 tests/unit/to-recognition.spec.ts      T015 tests/unit/map-google-error.spec.ts
T016 tests/contract/google-vision.spec.ts   T017 tests/unit/sniff-format.spec.ts
T018 tests/contract/image.spec.ts           T019 tests/unit/split-reply.spec.ts
T020 tests/contract/shopping-list.spec.ts   T021 tests/contract/discord-routing.spec.ts
T022 tests/contract/lifecycle-ocr-wiring.spec.ts
T023 tests/integration/list-ocr.spec.ts

# Once RED, launch the pure impls in parallel:
T024 src/google-vision/to-recognition.ts    T025 src/google-vision/map-google-error.ts
T026 src/discord/split-reply.ts             T027 src/image/sniff-format.ts

# Then the sequential chain:
T028 src/image/fetch-image.ts → T029 src/google-vision/provider.ts
→ T030 src/shopping-list/handle-list-submission.ts → T031 src/discord/adapter.ts
→ T032 src/lifecycle/run-app.ts
```

---

## Implementation Strategy

### MVP First (User Story 1 Only)

1. Complete Phase 1: Setup
2. Complete Phase 2: Foundational (CRITICAL — blocks all stories)
3. Complete Phase 3: User Story 1
4. **STOP and VALIDATE**: run `tests/integration/list-ocr.spec.ts` — photo-in → byte-faithful text-out through the real adapter/handler against stubs; then, with real GCP credentials, quickstart live smoke scenarios 1–2 and 9
5. This is the MVP: single/multi-image recognition with faithful pass-through, usage hint, splitting, mention neutralization, and allowlist. Failure handling is coarse (every non-ok outcome → the generic message); US2 refines it

### Incremental Delivery

1. Setup + Foundational → contract, config, messages, stubs ready
2. + US1 → the recognition round trip works end to end (**MVP**)
3. + US2 → the full three-class outcome taxonomy, busy guard, budget enforcement, content-free transition logs — the Constitution's graceful-degradation constraint is only fully satisfied here, so ship US2 immediately after US1
4. + US3 → swap-by-configuration proven; operator docs complete
5. + Polish → SC-005 log scan, full verification gate, live smoke 1–13, VPS validation

### Sequential Note

Unlike 001, the three stories are NOT parallelizable across developers: US2 edits US1's orchestrator file, and US3's parity suite asserts US1+US2 behavior. Parallelism lives WITHIN each phase (see Parallel Opportunities).

---

## Notes

- [P] = different files, no dependency on an incomplete task
- Story labels map tasks to spec user stories for traceability; Setup/Foundational/Polish carry none
- Hardcoded constants are module constants, NEVER config: `MAX_IMAGE_BYTES = 7 * 1024 * 1024` (src/image/fetch-image.ts), `SUBMISSION_BUDGET_MS = 25_000` (src/shopping-list/handle-list-submission.ts), split limit `2000` (src/discord/split-reply.ts)
- `@google-cloud/vision` importation is machine-enforced to `src/google-vision/` only via biome.json (T003) — the same zone pattern as 001's discord.js/pino/zod boundaries
- No test contacts the real Vision API or Discord gateway; live behavior is validated only in the quickstart smoke run (T041) and on the VPS (T042)
- Code comments and test names MUST NOT reference spec identifiers (FR-XXX, SC-XXX, contracts/*.md, R-n, T-n) — describe the behavior directly, or use the feature name `002-shopping-list-ocr` (AGENTS.md / Constitution workflow §6)
- Commit after each task or logical group using the `commit` skill; never commit `.env` or key material
- After code changes run `npm run typecheck`, `npm test`, and `npm run lint` (AGENTS.md) — T040 is the consolidated gate
