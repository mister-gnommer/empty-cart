# TODO

## Current

- check /tests/contract and /tests/helpers
  - /tests/contract/discord.spec.ts done
  - ask for all tests to use  `assert.ok(failLine);` wherever possible to remove assertions
- all ready? Was there speckit command to run to update spec after code changes?
